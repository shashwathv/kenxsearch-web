/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',   // uncomment for production/Vercel deploy
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
